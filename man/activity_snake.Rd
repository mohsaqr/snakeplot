% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/activity_snake.R
\name{activity_snake}
\alias{activity_snake}
\title{Activity Snake Plot}
\usage{
activity_snake(
  data,
  band_height = 28,
  band_gap = 18,
  day_start = 360,
  day_end = 1440,
  plot_width = 500,
  band_color = "#3d3d4a",
  event_color = "#d4a843",
  arc_color = "#2a2a3a",
  band_opacity = 0.9,
  arc_opacity = 0.85,
  event_opacity = 0.85,
  min_tick_width = 1.5,
  show_grid = TRUE,
  show_total = TRUE,
  show_count = FALSE,
  show_hour_labels = TRUE,
  show_arc_labels = TRUE,
  shadow = TRUE,
  grid_color = "rgba(255,255,255,0.25)",
  label_color = "#cccccc",
  label_cex = 0.85,
  label_align = "left",
  orientation = c("horizontal", "vertical"),
  start_from = c("left", "right"),
  day_format = NULL,
  legend = NULL,
  title = NULL,
  margin = c(top = 30, right = 10, bottom = 50, left = 80),
  bg = "white"
)
}
\arguments{
\item{data}{Input in one of three formats.
(1) \strong{POSIXct vector}: a bare vector of timestamps, producing rug
ticks grouped by day.
(2) \strong{Numeric format}: a data.frame with columns \code{day}
(character/factor day label), \code{start} (numeric minutes from midnight,
0--1440), \code{duration} (numeric minutes; 0 = rug tick), and optionally
\code{label} (character event label).
(3) \strong{Timestamp format}: a data.frame with POSIXct column
\code{timestamp} (or \code{start}), optionally \code{end} (POSIXct; if
present, duration is computed), \code{duration} (numeric minutes; used when
\code{end} is absent), and \code{label} (character event label).}

\item{band_height}{Numeric. Height of each day band in plot units
(default 28).}

\item{band_gap}{Numeric. Vertical gap between bands (default 18).}

\item{day_start}{Numeric. Start of the time window in minutes from midnight
(default 360 = 6AM).}

\item{day_end}{Numeric. End of the time window in minutes from midnight
(default 1440 = midnight).}

\item{plot_width}{Numeric. Width of the band area in plot units
(default 500).}

\item{band_color}{Character or character vector. Band ribbon color(s).
If a vector, colors cycle per day (default "#3d3d4a").}

\item{event_color}{Character or character vector. Event tick/block color(s).
If a vector, colors cycle per day (default "#d4a843").}

\item{arc_color}{Character. Overnight arc color (default "#2a2a3a").}

\item{band_opacity}{Numeric 0-1 (default 0.90).}

\item{arc_opacity}{Numeric 0-1 (default 0.85).}

\item{event_opacity}{Numeric 0-1 (default 0.85).}

\item{min_tick_width}{Numeric. Minimum event width in plot units
(default 1.5). Use 1.0 for thin rug style.}

\item{show_grid}{Logical. Show hour gridlines (default TRUE).}

\item{show_total}{Logical. Show total duration after day label
(default TRUE).}

\item{show_count}{Logical. Show event count in parentheses after day label
(default FALSE).}

\item{show_hour_labels}{Logical. Show hour labels at bottom (default TRUE).}

\item{show_arc_labels}{Logical. Show "12AM" at arc tips (default TRUE).}

\item{shadow}{Logical. Draw drop shadows (default TRUE).}

\item{grid_color}{Character. Gridline color
(default "rgba(255,255,255,0.25)").}

\item{label_color}{Character. Day label color (default "#cccccc").}

\item{label_cex}{Numeric. Label font size multiplier (default 0.85).}

\item{label_align}{Character. Label alignment: "left" (default), "right",
or "direction" (follows band reading direction).}

\item{orientation}{Character: "horizontal" (default) or "vertical".
Controls whether the snake runs left-right or top-bottom.}

\item{start_from}{Character: "left" (default) or "right". Which side the
first band starts from.}

\item{day_format}{Optional strftime format for day labels when \code{start}
is POSIXct (e.g., \code{"\%a"} for "Mon", \code{"\%Y-\%m-\%d"} for
dates). NULL = auto-detect (\code{"\%a"} for 7 or fewer days,
\code{"\%Y-\%m-\%d"} otherwise).}

\item{legend}{List of legend items, each with \code{label} and
\code{color}. NULL for no legend.}

\item{title}{Optional plot title.}

\item{margin}{Named numeric vector with top, right, bottom, left margins.}

\item{bg}{Background color (default "white").}
}
\value{
Invisible \code{snake_layout} object (for downstream use).
}
\description{
A daily activity timeline where each band is one day and colored
ticks or blocks on a dark ribbon represent events. The serpentine
(boustrophedon) layout connects days via U-turn arcs.
}
\examples{
# Weekly rug-style activity plot
set.seed(42)
days <- c("Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun")
d <- data.frame(
  day      = rep(days, each = 40),
  start    = round(runif(280, 360, 1400)),
  duration = 0
)
activity_snake(d)

# Duration blocks
d2 <- data.frame(
  day      = rep(days, each = 8),
  start    = round(runif(56, 360, 1200)),
  duration = round(runif(56, 15, 120))
)
activity_snake(d2, event_color = "#e09480", band_color = "#3d2518")

}
